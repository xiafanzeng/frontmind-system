/**
 * Server compatibility entry point. The syntax-only repair core is shared
 * with browser-side knowledge-base projection so both trust boundaries use
 * exactly one implementation.
 */
export * from "../shared/model-output-repair";
