# 知识库上传、连续补充与常驻成果栏验收

日期：2026-09-11。代码基线：`f31e5902576d0ebea10656320b7a99ff3b003e63`；候选分支：`codex/kb-upload-reset-final`。

真实智谱闭环已完成：9 份资料全部处理，48 个节点、48 个证据文件、连续补充、事实溯源、正式 ZIP 和实际费用均已核验。生产发布状态见最终交付。真实模型与供应商模拟结果分别记录。

## 已修复与验证

| 范围 | 修复和验收 |
|---|---|
| 上传身份 | actor/owner、默认及非默认企业项目、授权工程师、冻结凭据读取统一；真实事务验证越权、凭据轮换/撤销和重置隔离 |
| 独立批次 | 初建、补充、重选使用管理器；连续新请求释放旧预约/Promise/文件；同轮继续保持已确认文件 |
| 恢复边界 | 预约/暂存/保留文件确认/派发有30秒核对和6分钟最终边界；未知派发只核对同轮；最新attempt用于后续请求 |
| 迟到响应 | 已拒绝的旧控制响应不能解除新停止；换批次取消旧状态请求；暂存已提交时状态确认可结束原等待分支 |
| 状态与用量 | upload-status、observation及执行区共用阶段；停止未调用不展示调研；按build+generation分组，保留当前轮与历史调用区别 |
| 连续Low编辑 | 完成的本地节点Patch使用真实上传来源证明，不再错误要求旧供应商附件请求；同节点两次、另一节点一次各自成功 |
| 补充费用 | Low编辑intent关联其真实turn；默认项目兼容旧空项目字段；已记录费用事件证明调用，按消息顺序选最新轮次 |
| 持久图片 | 按用户/build/generation/hash读取持久原始资料，临时上传副本缺失仍可预览和封存；错代、损坏字节拒绝；最终ZIP视觉校验读取已封存证据 |
| 成果栏 | 桌面两列默认5/7与2/7；导航208/56px；桌面/平板浮窗与主区等高（包括空状态），手机上方96–260px；删除抽屉和整体隐藏入口；拖动、双击/Enter恢复、独立滚动 |
| 其他模块 | 保留真实业务阶段、每轮最终回答复制、共享分隔线与发布页全路由白底；不以本地预约伪装供应商受理 |

## 自动化检查

- 全量 `pnpm --filter frontmind-client test`：最终服务端4508通过、130跳过；客户端1698通过、7跳过。
- 全量之后的管理器/恢复修复：专项33项及相关231项通过；持久图片/预览/绑定/下载/进度/会话专项223项通过。
- `pnpm typecheck` 与 `check:governance` 通过；治理只有既有 `dashboard-api.ts`、`knowledge-base-api.ts` 两项 advisory 行数提示。
- `git diff --check` 通过；没有新增或修改迁移文件。
- 最后客户端全量曾暴露 QuestionsWorkflow 成果摘要 effect 的异步断言竞态；测试改为等待可见最终摘要（保留实体/内容断言），全量重跑通过。
- required MySQL：2个测试文件、4项通过、0跳过。最后修订重跑的首次启动因指定空数据库尚未创建而失败，创建专用隔离空库后通过；未连接生产数据库。
- `pnpm build` 首次因生产构建要求源码已提交而停止（`BUILD_SOURCE_NOT_COMMITTED`）；候选提交后执行构建。候选 `3f9f380` 构建和生产 bundle audit 通过；最终源码提交后再次构建。

普通测试跳过项沿用条件配置或已有显式skip，未将跳过视为通过：MySQL、跨仓库配置和浏览器集成环境条件；已退役知识库恢复路径；未启用的旧编辑器/发布路径。知识库required MySQL独立执行，环境缺失会失败。

## 真实MySQL（供应商边界模拟）

使用专用空MySQL数据库、真实登录、真实资料上传和业务事务。初建供应商HTTP及Low编辑供应商HTTP分别模拟；没有替换上传凭据解析、暂存、停止、重置或Low运行日志/计费实现。

- 初建只创建一次供应商任务，完成知识节点确认和最终ZIP生成/下载。
- 同节点两轮及不同节点一轮补充：3个不同turn、3个新Low session、3次模型command。重复预约/派发不增加创建和调用。
- 三次Low调用的合成供应商用量：输入360 Token、输出240 Token；真实测试账本合计96万分元（0.0096元），按同一build/generation归集、明细分别绑定3个turn，状态已同步。刷新重读一致。
- 补充后再次本地确认，最终ZIP生成和已登录下载成功。
- 上述用量和产物来自受控供应商模拟，不是实际模型消耗或真实模型事实核验。

## 浏览器与视口

使用候选客户端、独立本地Dashboard、隔离数据库和真实测试登录。

- 重置前旧批次停止；重置后旧turn查询409。
- 9份有效合成资料共159,600,000 bytes。第1份确认后切到通用智能体，第2–4份继续上传；停止后刷新保持4/9。
- 显式继续生成新attempt；重选原文件只发送第5–8份，停止/刷新保持8/9。
- 再次显式继续后只发送第9份；暂存真实提交后保持响应未返回并执行停止，刷新保持停止和9/9，派发请求数0。
- HTTP延迟/保留响应仅用于控制时序；所有文件上传、暂存和停止请求均进入真实本地后端。
- 390、767、768、769、1023、1024、1025、1279、1280、1281、1440、1920px：知识库与词库、问题、应答逻辑、内容、官网、监控、发布、问答页面无横向溢出，成果区常驻。
- 内部详情连续收展10次：阅读锚点偏差0px，对话列宽偏差0px。
- 本次没有声称全部断网/手机真实键盘/真实模型派发切页场景已在浏览器覆盖。

本地证据目录：`/tmp/frontmind-kb-final/`。结果文件为 `browser-upload-result.json`、`browser-resume-result.json`、`browser-final-upload-result.json`，视口为 `layout/modules.json`、`layout/anchor.json` 及截图。账号、cookie、密钥、环境文件和测试资料不进入仓库。

## 真实智谱模型与浏览器闭环

隔离实例 `127.0.0.1:5198`，专用项目“澄川资料科技 · 知识库闭环测试”，项目 ID `07fa2733-1f46-89b2-a669-8306c9c317f5`。使用用户授权的智谱连接；模型 HTTP 未模拟。

- 全新重置后旧 turn 返回 409；9 份有效合成业务文本总计 159,600,000 bytes。
- 第 1 份确认后切至通用智能体，其余 8 份继续上传；9 份全部确认前未派发，确认后只创建一个初建 session 和一个模型 command。
- 初建 turn：`afe18bab-7afa-4b7a-9efa-9f7dd646102c`，build：`cd98b04e-3f24-4ca5-9362-ae476bc3c2d1`，generation 1。
- 生成 48 个知识节点、48 个证据文件；模型读取了全部 9 份资料。浏览器“确认初稿，进入编辑”成功，48 个节点均进入可编辑状态。
- 三项事实：成立于 2018 年 6 月 15 日、总部浙江省杭州市余杭区、核心产品“澄川档案云”。逐项核对节点正文和 ZIP 内资料/引用，全部可追溯。资料中的业务台账均明确标注合成属性。
- 初建 ZIP：159,705,337 bytes，CRC 校验成功；SHA-256 `39049e650f34511dc51c719108443046b9b9f4031bf072d8d59bfe79052188a3`。
- 初建实际费用：输入 108558、输出 67171、缓存输入 989440 Token；账本 47281 万分元（4.7281 元），用量状态 `synced`。9 个纯附件传输项不再显示为独立 AI 任务；真实初建费用完整保留。
- 真实补充首轮发现预约回包前后 `activeTurnResetRevision` 从缺省 0 变为 1 导致错误退役上传执行器。保留原失败记录、原预约和已保存附件，沿同一 turn 恢复，未新建替代任务；浏览器恢复不重传字节，实际模型追加事实成功。修复为使用工作区权威 reset revision，活动轮次释放不再改变上传批次；补齐实际 Hook 上传中重渲染回归。
- 最终真实浏览器连续三轮（同一页面会话）：节点 `1.0` 两次、节点 `1.1` 一次，分别为 turn `cea916a5-9caf-47f0-9423-78f1d3431288`、`a2766eba-f73b-43d0-b798-17436f3a675a`、`d8345e5b-bdc2-4345-9d39-897e158f686a`。3 个独立预约、3 个新 session、3 次模型 command，重复派发均未增加任务；第一轮切到通用智能体后完成，返回后继续第二轮。正文版本依次为 3、4、5，前轮补充完整保留，最终刷新读取一致；浏览器 pageerror 为 0。
- 包括原预约恢复，共 4 次真实 Low 补充，加上 1 次初建，总计 5 个 session、5 个 settled command；输入 111154、输出 68168、缓存输入 995520 Token，实际账本合计 47889 万分元（4.7889 元）。费用按同一 build/generation 归集，各 turn 分列；最终真实接口汇总与 5 条调用明细均为 `synced`。本地确认/选择节点有明确零 providerRequestCount 证据时显示未调用，真实未知派发仍保留待确认状态。
- 补充后在浏览器确认两个节点，48/48 全部确认，点击“更新知识库”及“确认更新”生成并启用正式知识版本。状态 `published`、contentVersion 5；最终发布 ZIP 为 75,747 bytes、108 个条目，CRC 校验通过，三项原始事实和四项补充事实均在对应正文/证据中找到。SHA-256：`02db73deab38cdc1dc4b0672f4d8705aae6e492140418907159a1746b53a7597`。
- 用户最新要求的浮窗全高：390、767、768、769、1023、1024、1025、1279、1280、1281、1440、1920px 均检查；≥768px 成果区与主区等高且无横向溢出，手机成果区 96–260px。

证据目录 `/tmp/frontmind-kb-live/`：`browser-upload-result.json`、`initial-review.json`、`initial-accepted.json`、`initial-node-details.json`、`usage-ledger.json`、`usage-verified.json`、`supplement-first-recovered.json`、`supplements-result.json`、`final-package-review.json`、`layout/height.json` 和对应截图。凭据、账号、测试资料和隔离数据库不进入 Git 或生产。

## 发布状态与剩余条件

目标是 `dashboard.frontmind.cn` 的Dashboard、siteops-worker和monitoring-worker。发布前生产基线为Dashboard/siteops `f31e590`、monitoring `f0f0379`。本文件记录发布前已完成的源码和真实业务验收；生产SHA、不可变镜像digest、服务状态、线上smoke和回滚版本记录在本次发布任务的最终交付。

真实模型闭环（初建、恢复、连续补充、最终正式版本 ZIP 和费用）已通过。最终源码构建和生产已登录 smoke 在发布时核实并记录于最终交付。通过后按已授权三步流程发布一个main SHA，使用生产独立构建的镜像；本次无迁移，启动后检查健康、就绪、SHA/digest和已登录生产专用测试项目。只有这些全部通过才可声明修复完成并上线。
