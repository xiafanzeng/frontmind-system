# 操作员工作区迁移记录

新增迁移按 journal 的连续索引 61–64 顺序执行，SQL 文件名为 0062–0065。0061 保留缺号；journal 检查允许递增文件编号的缺号，同时继续拒绝乱序、重复编号及不连续的 journal 索引。既有迁移及快照不改写。

| Journal 索引 | SQL                                  | 当前阶段的 Dashboard 契约表数 |
| ------------ | ------------------------------------ | ----------------------------- |
| 61           | `0062_enterprise_operator_projects`  | 92                            |
| 62           | `0063_unified_account_wallet`        | 96                            |
| 63           | `0064_ai_cost_accounting`            | 101                           |
| 64           | `0065_publisher_enterprise_projects` | 101                           |

每阶段快照准确保留当时已存在的表，并使用连续 `prevId` 链。0063 开始将 `monitoring_users` 纳入契约，以验证统一钱包的账号外键。依照现有融合部署设计，其余 monitoring/publisher 表继续由 SQL 迁移管理，不纳入 Dashboard Drizzle 快照；0065 的 Dashboard 表结构因此与前一阶段相同。发布流程已有 `FRONTMIND_SCHEMA_CONTRACT_ALLOW_EXTRA_TABLES=1` 支持该边界。

四个迁移均包含数据回填，按项目现有规则登记为 `contract`。部署时先停止 Dashboard 和使用资金表的工作线程，再执行迁移，随后使用支持统一钱包的版本启动。迁移保留旧钱包、业务资金流水和不可变内容记录；0063 在停写期间冻结历史两钱包的合并期初数。回滚资金处理遵循统一钱包的运行时版本保护，不能用旧计费程序重新写入已切换的账户。

## 已完成验证

2026-09-07 在本机 MySQL 33077 新建的隔离数据库 `fm_operator_full_acceptance_20260907` 从空库执行全部 65 个 journal 迁移，共 1,222 条 SQL 语句成功。基线后插入合成账户、历史媒体稿件及两钱包金额，升级后核验：稿件归入该账户默认企业项目；余额 20,000 + 10,000 = 30,000，预留 3,000 + 1,000 = 4,000，冻结 500，累计消耗 4,000 + 2,000 = 6,000（单位均为万分之一元）。

最终 101 张契约表与 `information_schema` 的列、默认值、索引、外键及 CHECK 对比为 `exact`，期望与实际哈希一致。测试还检查每个新增迁移的断句、政策分类和快照阶段，发布 manifest 能覆盖全部 SQL 哈希。

完整 append-only CLI 已通过。检查暴露了历史 0055–0058 的分类与 SQL 不一致：这些迁移包含约束收紧、种子写入或 `MODIFY`，因此仅将政策元数据修正为 `contract`，不改写历史 SQL、哈希或快照。0059–0060 的新增列使用 `DEFAULT 'manus' NOT NULL`；检查器现已接受这一等价列属性顺序，继续拒绝表达式默认值、约束收紧及数据写入。相关 61 个测试通过，并新增仓库所有 `expand` 分类与严格 SQL 检查的一致性回归测试。
