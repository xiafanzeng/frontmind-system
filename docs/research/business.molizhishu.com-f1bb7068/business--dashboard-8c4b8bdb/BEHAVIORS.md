# 行为与边界

原站只读观察；未点击保存、立即执行、品牌创建、真实导出、推荐问题或可能付费的操作。带参数URL作为独立状态。监控草稿取消关闭。HelpLook无保存按钮的即时开关未修改。

本地使用合成演示数据，保存/执行明确反馈演示结果，不调用参考站。真实工作区沿用既有服务端与项目归属校验。新官网设置只保存当前账号、项目的本地草稿；嵌入代码仅作为文本，不执行。WordPress、文章发布、阿里云流程待定义，不伪造成功。

待验证：2048px同视口逐状态截图、全量Hover/Pressed计算样式。当前原生窗口截图不能支持逐像素一致声明。
