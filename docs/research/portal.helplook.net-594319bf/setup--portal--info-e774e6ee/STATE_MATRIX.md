# 状态矩阵

| 状态 | 原站观察 | 本地验证 |
|---|---|---|
| H0 | 站点信息、模板与布局 | 默认、编辑器/主题/模板选择、独立表单滚动 |
| H1–H8 | SEO、域名、多语言、嵌入、文章、栏目、搜索、通知 | 全部Tab点击、选中态、控件与保存 |
| H9 | 导航栏（已补稳定截图） | 新增、编辑、上移、顺序预览 |
| H10 | 主页（已补稳定截图） | 手风琴开合、标语、颜色、开关、预览 |
| H11 | 页脚关闭禁用（已补稳定截图） | 关闭禁用、开启编辑、预览 |
| H12 | 站点语言展开 | 语言、主题、模板选中 |
| H13 | 不在原站提交验证 | 本地空名称错误，阻止保存 |
| H14 | 未使用原站发布 | 本地预览打开/关闭，草稿保留 |
| H15 | FrontMind新增适配 | 官网四子板块、待接入状态 |
| H16 | 未取得完整原站computed CSS | 本地hover、pressed、键盘Tab切换 |

`apps/dashboard/scripts/operator-portal-ui-smoke.mjs` 在2048×1200 Chromium捕获22份截图与逐状态computed CSS，断言无API/外站请求、无运行错误；本地草稿刷新保留、错误不写入、对话框关闭均通过。截图及CSS在 `/tmp/frontmind-operator-portal-ui/`，不提交媒体。

原站使用当前登录Chrome原生截图（1329px窗口、110%缩放）。浏览器扩展DOM/CDP持续超时；完整2048px同视口原站对比和所有控件computed CSS尚未完成，不能将本地交互通过声明为像素级复刻通过。
