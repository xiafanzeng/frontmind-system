# Molizhishu provider adapter

Source-preserving copy of `packages/provider-moli` from
`frontmind-monitoring-system`. Credentials and endpoints are supplied only via
runtime configuration; no secret values are committed.
