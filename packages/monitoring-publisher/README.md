# Media publishing domain module

Source-preserving copy of `packages/publisher` from
`frontmind-monitoring-system`, retained because the monitoring worker's media
publishing jobs depend on its canonicalization and state-machine contracts.
Provider credentials remain runtime-only.
